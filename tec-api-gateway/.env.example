# API Gateway Configuration
PORT=3000
NODE_ENV=production

# Service URLs — prefer Railway internal DNS for private service-to-service traffic.
# Replace <project-id> with your Railway project ID.
# Example internal DNS:  http://<service-name>.railway.internal:<port>
AUTH_SERVICE_URL=http://auth-service.railway.internal:5001
WALLET_SERVICE_URL=http://wallet-service.railway.internal:5002
PAYMENT_SERVICE_URL=http://payment-service.railway.internal:5003

# Internal auth secret — shared between the gateway and all downstream services.
# Every proxied call includes the header `x-internal-key: <INTERNAL_SECRET>`.
# Services reject requests that omit this header or provide the wrong value (HTTP 403).
# Generate a strong random value: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
INTERNAL_SECRET=

# CORS — comma-separated list of allowed origins for browser clients.
# Supports exact origins and wildcard sub-domain patterns (e.g. https://*.vercel.app).
# Leave empty to deny all cross-origin browser requests (recommended for production).
# Example: ALLOWED_ORIGINS=https://tec-app.vercel.app,https://*.vercel.app
ALLOWED_ORIGINS=https://tec-app.vercel.app,https://*.vercel.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging (error, warn, info, debug)
LOG_LEVEL=info

# Service name — included as a label in all structured log lines and metrics.
SERVICE_NAME=api-gateway

# Sentry DSN — leave blank to disable error tracking (disabled automatically in development/test).
# Obtain from https://sentry.io → your project → Settings → Client Keys (DSN).
SENTRY_DSN=

# Service version (optional, shown in /health response)
SERVICE_VERSION=1.0.0
