# Phase 1 & 2: Infrastructure Setup and Pi Network Configuration

This PR adds a global `.env.example` with Pi Network configurations (testnet/mainnet switch) and introduces a root `package.json` utilizing npm workspaces to streamline managing the microservices.